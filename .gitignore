# Node dependencies
node_modules/

# Environment variables
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# MySQL (optional: keep jewelry.sql if it's your schema, but avoid real user data)
# If jewelry.sql contains sample data only, you can keep it (we'll commit it)
# But avoid committing files with real customer data later

# React build
/frontend/build